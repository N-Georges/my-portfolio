import project from "./project-schema";
import category from "./category-schema";
import guestbook from "./guestbook-schema";

const schemas = [project, category, guestbook];

export default schemas;
